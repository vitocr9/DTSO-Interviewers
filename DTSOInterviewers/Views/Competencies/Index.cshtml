@model IEnumerable<DTSOInterviewers.Models.Competency>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Stakeholder)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Stakeholder)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCompetency }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCompetency }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCompetency })
        </td>
    </tr>
}

</table>
